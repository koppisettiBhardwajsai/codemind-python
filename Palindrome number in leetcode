bool isPalindrome(int x){
   long long int r,rev=0;
   long long int t=x;
   while(x>0 )
   {
       r=x%10;
       rev=rev*10+r;
       x=x/10;
   }
   if(t==rev)
   {
       return true;
   }
   else
   {
       return false;
   }
}
